apiVersion: 1

providers:
  - name: Kafka    # A uniquely identifiable name for the provider
    folder: Kafka # The folder where to place the dashboards
    type: file
    updateIntervalSeconds: 10
    options:
      path:  /etc/grafana/provisioning/dashboards/kafka
