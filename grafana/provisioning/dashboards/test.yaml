apiVersion: 1

providers:
  - name: Test    # A uniquely identifiable name for the provider
    folder: Test # The folder where to place the dashboards
    type: file
    updateIntervalSeconds: 10
    options:
      path:  /etc/grafana/provisioning/dashboards/test
